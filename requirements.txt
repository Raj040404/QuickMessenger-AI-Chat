# Django-related dependencies
Django>=3.2
djangorestframework

# AI and HTTP libraries
openai
requests

# JSON handling and parsing
jsonlib-python3

# Virtual environment management (optional)
virtualenv

# LM Studio related (if any additional packages are needed to interface with the local server)
# (Specify any dependencies for LM Studio interaction, if applicable)

# Other dependencies (add as required)
gunicorn    # For deploying with WSGI server (optional)
whitenoise  # To serve static files (optional)
